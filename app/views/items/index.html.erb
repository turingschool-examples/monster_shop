<% if @merchant %>
  <h1>All Items From <%= link_to @merchant.name, merchant_dashboard_path %></h1>
  <%= link_to 'New Item', new_item_path %>
<% else %>
  <h1>All Items</h1>
<% end %>

<%= render 'items' %>
